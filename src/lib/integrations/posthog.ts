/**
 * PostHog integration placeholder.
 *
 * When ready:
 * 1. npm install posthog-js
 * 2. Call initPostHog() from a client provider
 * 3. Set NEXT_PUBLIC_POSTHOG_KEY and NEXT_PUBLIC_POSTHOG_HOST
 */
export function initPostHog(): void {
  if (process.env.NEXT_PUBLIC_POSTHOG_KEY) {
    // TODO: Initialize posthog-js
  }
}
